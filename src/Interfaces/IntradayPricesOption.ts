export interface IntradayPricesOption {
  chartIEXOnly: boolean;
  chartReset: boolean;
  chartSimplify: boolean;
  chartInterval: number;
  changeFromClose: boolean;
  chartLast: number;
  exactDate: string;
  chartIEXWhenNull: boolean;
}
